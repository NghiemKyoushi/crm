export type User = {
  id: string;
  user_name: string;
};

export type UserProfile = {
  email: string;
  phone_number: string;
  full_name: string;
  birthday: string;
};
export type Password = {
  old_password: string;
  new_password: string;
};
